# monitoring/grafana/provisioning/datasources/datasource.yml
# Grafana datasource configuration for AutoOps monitoring
# This file sets up Prometheus as a datasource in Grafana.

---
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true